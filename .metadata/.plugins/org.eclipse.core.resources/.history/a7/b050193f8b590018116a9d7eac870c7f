package com.bizleap.training.ds.loader.impl.test;

import static org.junit.Assert.assertTrue;

import org.apache.log4j.Logger;
import org.junit.Ignore;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

import com.bizleap.training.ds.loader.DataManager;
import com.bizleap.training.ds.loader.impl.test.ServiceTest;

public class DataManagerImplTest extends ServiceTest{
	
	@Autowired
	private DataManager dataManager;
	static Logger logger=Logger.getLogger(DataManagerImplTest.class);
	
	//@Ignore
	@Test
	public void testLoad() {
		//DataManagerImpl dataManagerImpl=new DataManagerImpl();
		logger.info("TEST");
		dataManagerImpl.load();
		
	/*	assertNotNull(dataManagerImpl.getEmployeeList());
		assertTrue(CollectionUtils.isNotEmpty(dataManagerImpl.getEmployeeList()));
		assertEquals(3,dataManagerImpl.getEmployeeList().size());
		
		assertNotNull(dataManagerImpl.getCompanyList());
		assertTrue(CollectionUtils.isNotEmpty(dataManagerImpl.getCompanyList()));
		assertEquals(3,dataManagerImpl.getCompanyList().size());*/
	}
	
	@Ignore
	@Test
	public void testLoadFalse() {
		assertTrue(false);
	}

}
